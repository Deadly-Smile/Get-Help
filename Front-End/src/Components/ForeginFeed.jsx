/* eslint-disable react/prop-types */
export const ForeginFeed = ({ width, height }) => {
  return (
    <div
      style={{
        width: `${width}px`,
        height: `${height}px`,
      }}
      className="md-0 bg-green-100 hover:bg-red-100 text-center"
    >
      ForeginFeed
    </div>
  );
};
